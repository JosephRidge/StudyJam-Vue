import { createApp } from 'vue'
import './style.css' // imports styling 
import App from './App.vue' // imports the enrty point to our vue application

createApp(App).mount('#app') // creates first instance of application.
